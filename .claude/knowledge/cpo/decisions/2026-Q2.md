# CPO 判断記録 2026-Q2

## 2026-04-25 — ペインコマンド存在チェック仕様反映 — specification.md §補助機能に3項目目として追記

### 対象

check_command() ヘルパー関数の追加と cmd_new() 内での VIBEMUX_PANE_LEFT / VIBEMUX_PANE_RIGHT 実行時コマンド存在チェック実装に伴う、docs/specification.md §補助機能の不整合修正。

### 判断

**specification.md §補助機能に「ペインコマンド存在チェック」を3項目目として追記。**

### なぜそう判断したか

- 同日（2026-04-25）の lazygit 未インストール対策案の判断で、案2（実行時チェック）を必須と判定済み
- 今回の実装はその判断を正確に実現しており、プロダクト方針と整合
- VIBEMUX_PANE_LEFT / VIBEMUX_PANE_RIGHT に対して汎用チェックを行う実装は Value #3「道具に縛られない」に準拠
- 仕様書と実装の不整合はプロダクトの Source of Truth としての信頼性を損なうため、コード実装に合わせて仕様を更新した


## 2026-04-25 — PR #20/スキル設計パターン — レビュー指摘から3原則を設計思想に昇格

### 対象

PR #20 のレビューコメント（reviewer + CodeRabbit）から抽出したスキル設計パターン。

### 判断

以下3点をスキル設計原則として `docs/design-philosophy.md` に追記する（MUST/SHOULD）。

#### 1. git 操作のスコープ一貫性（MUST）

`git status` の検知スコープと `git add` のスコープを一致させること。

- CodeRabbit 指摘（PR #20, id:2964387129）: `review-to-rules` 実行後の変更検知が `git status --porcelain`（全体）で、`git add` が `.claude/rules/ .claude/knowledge/` に絞られている不整合を発見
- 不一致だと「検知はするが追加されない」変更が生じ、後続ステップが不正な前提で進む
- MVV バリュー「フローを切らない」— 不整合による意図せぬ失敗はフローを中断させる

#### 2. ベースブランチ名のハードコード禁止（MUST）

スキル内で `git checkout main` を禁止し、`{baseRefName}` 等で動的取得すること。

- PR #20 の修正コミット（05dd4969）で `git checkout main` → `git checkout {baseRefName}` に修正
- CodeRabbit が承認（id:2964378473）: `develop`, `staging` 等のベースブランチでも正しく動作するようになった
- MVV バリュー「道具に縛られない」— 特定のブランチ名戦略に縛られるスキルは汎用性を損なう

#### 3. グローバルスキル依存のフォールバック明示（SHOULD）

スキルが他のグローバルスキルに依存する場合、利用不可時のフォールバック手順を SKILL.md に明記すること。

- PR #20 の対応（id:2964377599）: `/commit` はグローバルスキル定義済みだが、直接 `git commit` のフォールバックも追記
- CodeRabbit が承認（id:2964379270）: 「スキルが利用できない場合でも安全に動作する」と評価
- MVV バリュー「一発で整う」— スキル依存が原因で動かない状況は初期体験を損なう

### なぜそう判断したか

いずれも一過性の実装修正にとどまらず、**同種のスキル（SKILL.md）を新規作成・更新するたびに適用すべき汎用ルール**であるため、design-philosophy.md のスキル設計原則セクションに昇格させた。

特にスコープ一貫性とブランチ名動的取得は、CodeRabbit が繰り返し指摘する類のバグの根本原因であり、原則として明文化することで再発を防ぐ。

## 2026-04-25 — Issue #37 計画メタレビュー — specification.md 骨格再編計画を承認

### 対象

Issue #37 実装計画（`docs/specification.md` に Issue #30 の4問決着を反映）のCPOメタレビュー。

### 判断

**計画をOKとして承認。** 以下の点を確認済み。

1. SoT仕様書としての章構成再編（責務境界・推奨実行環境・並列時代のvibemux・ガードレール）は現行仕様書の欠落を正しく補うものであり妥当。
2. yazi デフォルト廃止の記述方針（specification.md では事実の記述のみ、Value #2 改訂はファウンダー判断として言及のみ）は `.claude/rules/mvv.md` に準拠。
3. #40/#41/#42 スタブの小見出し事前配置による並列衝突回避は妥当な設計。
4. 懸念事項C3・C4の対応（MVV改訂不介入・git addスコープ限定）はいずれも適切。

### 付帯条件

- Phase 3 タスク 3.2 において、Value #2 の文言と specification.md 内の「2ペインが人間の全活動をカバーする」等の断言との差異を明示的に確認し、必要であれば「Value #2 の定義はファウンダー判断で継続検討中」旨の一行留保を入れること。
- yazi廃止の「なぜ」の記述は、責務境界章またはペインレイアウト章の一箇所に集約することを推奨（読者の混乱回避）。

### なぜそう判断したか

計画全体が Issue #30 の決着内容を忠実に反映しており、MVV（特にValue #1・#3）との整合が取れている。懸念となりうるValue #2との張力については、計画が既にMVV改訂をファウンダー専権として正しく留保しており、CPOが追加介入する必要がない。スタブ設計も並列実行時の衝突リスクを最小化する合理的な選択。

## 2026-04-25 — lazygit未インストール対策案 — 案2（実行時チェック）必須、案1補助で両案採用

### 対象

`vibemux new` 実行時に `VIBEMUX_PANE_LEFT` のコマンド（デフォルト: lazygit）が未インストールの場合、左ペインで `command not found` が出る問題への対策案2つのMVV整合性判定。

- 案1: `make install` 時にコマンド存在を確認して案内表示（インストール続行）
- 案2: `vibemux new` 実行時にコマンド存在を `command -v` で確認し、なければエラー停止

### 判断

**案2（実行時チェック）を必須、案1（インストール時案内）を補助として、両案の採用を推奨。**

片方だけ選ぶなら案2を選ぶ。

### なぜそう判断したか

- **Value #1「一発で整う」**: 壊れた画面を黙って立てることは「一発で整う」の毀損。案2でエラー停止することで、何を直せばよいかを即座に示す。案1のみでは問題の発生点（実行時）に介入できない
- **Value #2「フローを切らない」**: 左ペインが死んだ状態のセッションは「今どうなっているか分からない」状態。案2の早期エラーの方が、沈黙したままの案1より誠実
- **Value #3「道具に縛られない」**: 実装上、確認対象は `lazygit` 固定ではなく `$VIBEMUX_PANE_LEFT` の値に対して汎用的に行うこと。specification.md §設定の設計と整合させる必要がある

### 実装注意

案2の `command -v` チェックは `$VIBEMUX_PANE_LEFT` に対して行い、lazygit をハードコードしないこと（Value #3 準拠）。
