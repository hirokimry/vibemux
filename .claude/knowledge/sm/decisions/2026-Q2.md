# SM 判断記録 2026-Q2

## 2026-04-24 — Issue #30〜#42 群 並列実行判定

### 対象 Issue

#42, #41, #40, #39, #38, #37, #35, #34, #31, #30, #26

### 判断内容

**#30 の状態**: コメントで4問の回答が完了しており実質設計完了。ただし OPEN のまま。downstream Issue の着手前にファウンダーへのクローズ確認を推奨。

**ファイル競合の主要ポイント**:
- README.md を触る: #26（スワップ）, #38（ヒーロー文）, #39（ガイド追加）→ 全て直列
- docs/specification.md を触る: #37（骨格）, #40, #41, #42（各論）→ #37 を先に実行

**自律実行不可領域**: 該当なし（#31 の PATH shim は新規ガードレール追加であり、既存ガードレールの緩和には非該当）

### 結論

```text
フェーズ 1（今すぐ並列可能）: #34 と #35 を並列、#26 を単独着手
フェーズ 2（#26 完了後）: #38 → #39 を直列
フェーズ 3（#30 クローズ確認後）: #37 を単独実行
フェーズ 4（#37 完了後）: #40, #41, #42 を並列
フェーズ 5（#34 完了後）: #31 を実装
```

### 保留

- #30: 実質完了だがファウンダーによる正式クローズが未実施

## 2026-04-24 — Issue #30〜#42 群 並列実行判定（詳細版）

### 追加分析: ファイル単位の競合マトリクス

| Issue | 対象ファイル | 競合する Issue |
|-------|------------|---------------|
| #26 | README.md（スワップ）, CLAUDE.md, .claude/agents/, .claude/skills/ | #38, #39 |
| #37 | docs/specification.md | #40, #41, #42 |
| #38 | README.md（ヒーロー文） | #26, #39 |
| #39 | README.md（ガイド追加） | #26, #38 |
| #40 | docs/specification.md | #37, #41, #42 |
| #41 | docs/specification.md | #37, #40, #42 |
| #42 | docs/specification.md | #37, #40, #41 |
| #34 | 設計ドキュメント（新規） | なし |
| #35 | 設計ドキュメント（新規） | なし |
| #31 | vibemux スクリプト | なし（#34 設計待ち） |
| #30 | 設計判断（コメントのみ） | — |

### #30 の扱いについての補足

#30 本体は OPEN だが、4問の設計回答がコメントで完了済み。
downstream（#37〜#42）は「#30 の決着を受けて」と明記されており、
実装の事前条件は満たされている。ただし正式クローズがないと
PR マージ後のトレーサビリティが薄れるため、ファウンダー確認を推奨。

## 2026-04-24 — Issue #26, #30〜#42 群 最大並列実行プラン（更新版）

### 変化点（前回 2026-04-24 判断からの差分）

- #35: マージ済み → 除外
- #34: 別エージェントが作業中（間もなく PR 作成見込み）→ 除外
- ユーザー指示: 「可能な限り最大並列」— 保守的な直列制約を緩和

### 再分析: セクション単位の競合評価

**README.md を触る Issue:**
- #26: README 全体スワップ（日英スワップ）
- #38: ヒーロー文（冒頭 L1〜L17付近）書き換え
- #39: 並列ガイド追加（末尾新セクション）

評価: #26 が完了し README が日本語版に差し替わった後なら、
#38（冒頭）と #39（末尾新セクション）は **異なるセクション** のため並列可。
#26 完了前に #38/#39 が走ると、全員が同一ファイルを別ブランチで編集し
マージ時にコンフリクトが生じる。→ #26 を先頭に置き、完了後 #38/#39 を並列起動。

**docs/specification.md を触る Issue:**
- #37: 骨格（責務境界・ガードレール等の新セクション群を追加）
- #40: VSCode 位置づけ追記
- #41: vibemux ポジショニング追記
- #42: Ghostty + tmux 推奨理由追記

評価: #40/#41/#42 はトピックが独立している。
#37 が先に骨格（空セクション）を作れば、#40/#41/#42 は異なるセクションに書き込むため並列可。
#37 なしに #40〜#42 が同時起動すると、3者が「どこに追記するか」を独自判断し重複コンフリクトのリスク。
→ #37 先行、完了後 #40/#41/#42 を並列起動。

**独立 Issue:**
- #30: OPEN だが設計完了済み。自身のファイル変更なし（コメントのみ）。downstream を進める前提として扱う。
- #26: CLAUDE.md, .claude/agents/, .claude/skills/ も触るが、
  他 Issue と競合するファイルは README.md のみ。

### 最大並列実行プラン

```text
ステップ1（今すぐ起動）:
  - #26（README スワップ + 言語方針統一）
    ※ README 系 Issue (#38, #39) のブロッカー

ステップ2（#26 マージ後 & #34 マージ後 — 並列起動）:
  - #37（specification.md 骨格）
  - #38（README ヒーロー文）  ← #26 完了後に起動可
  - #39（README 並列ガイド）  ← #26 完了後に起動可
    ※ #38 と #39 は同一ファイルの別セクション → 並列可
    ※ #37 は specification.md 骨格 → #40/#41/#42 の前提

ステップ3（#37 マージ後 — 並列起動）:
  - #40（VSCode 位置づけ）
  - #41（vibemux ポジショニング）
  - #42（Ghostty + tmux 推奨理由）

ステップ4（#34 マージ後）:
  - #31（PATH shim 実装）
```

### 保留

- #30: OPEN だが変更ファイルなし。downstream (#37〜#42) はすでに着手可。
  正式クローズはトレーサビリティのためファウンダー確認を推奨するが、blocking ではない。
- #34: 作業中。マージ後に #31 を起動。

## 2026-04-25 — Issue #26, #30〜#42 群 並列実行判定（再確認版）

### 対象 Issue

#26, #30, #31, #34, #37, #38, #39, #40, #41, #42

### 変化点（前回 2026-04-24 からの差分）

- worktree 確認: dev/26 と dev/34 のブランチが存在するが PR はまだゼロ
- dev/35 の worktree も存在するが #35 は前回確認時マージ済み扱い → 確認要
- dev/32, dev/33 の worktree が locked 状態で存在（別エージェントの作業）

### ファイル競合確認（git diff --name-only で実測）

| Issue/ブランチ | 実際の変更ファイル |
|--------------|----------------|
| #26 (dev/26_language-ja-first) | CLAUDE.md, README.ja.md, README.md |
| #34 (dev/34_kill-session-identification) | docs/SECURITY.md, docs/design-philosophy.md, docs/tmux-guardrail.md, docs/tmux-kill-session-identification.md（新規） |
| #37 (未着手) | docs/specification.md |
| #38 (未着手) | README.md |
| #39 (未着手) | README.md |
| #40 (未着手) | docs/specification.md |
| #41 (未着手) | docs/specification.md |
| #42 (未着手) | docs/specification.md |
| #31 (未着手) | vibemux（本体スクリプト） |

### 結論（最大並列実行プラン）

前回判断から変化なし。ファイル競合の実測でも前回の分析と整合している。

```text
今すぐ並列起動:
  - #26（README スワップ + 言語方針統一）
  - #34（kill-session 識別設計）← 独立して並列起動可

#26 マージ後（並列起動）:
  - #37（specification.md 骨格）
  - #38（README ヒーロー文・冒頭）
  - #39（README 並列ガイド・末尾）
  ※ #37 は specification.md、#38/#39 は README.md → 3者は並列可

#37 マージ後（並列起動）:
  - #40, #41, #42（specification.md 各論 3件）

#34 マージ後（独立タイミング）:
  - #31（PATH shim 実装）
```

### 保留

- #30: OPEN だが設計完了済み。変更ファイルなし。downstream の blocking にはならない。

## 2026-04-25 — Issue #30〜#42 群 並列実行判定（#26/#34/#53 マージ後の最終確認版）

### 対象 Issue

#30, #31, #37, #38, #39, #40, #41, #42

### 前提変化（今回の確認時点）

- #26（言語方針日本語統一）: main にマージ済み → README.md は日本語版に差し替え完了
- #34（kill-session 識別方式）: main にマージ済み → docs/tmux-kill-session-identification.md 追加済み
- #53（knowledge-pr）: main にマージ済み → docs/design-philosophy.md 更新済み、docs/specification.md は未変更

### ファイル競合マトリクス（Issue 本文から読み取り）

| Issue | 変更対象ファイル | 競合 Issue |
|-------|----------------|------------|
| #37 | docs/specification.md（骨格追記） | #40, #41, #42 |
| #38 | README.md（ヒーロー文・冒頭） | #39 |
| #39 | README.md（並列ガイド・末尾新セクション） | #38 |
| #40 | docs/specification.md（VSCode 位置づけ追記） | #37, #41, #42 |
| #41 | docs/specification.md（ポジショニング追記） | #37, #40, #42 |
| #42 | docs/specification.md（Ghostty+tmux 推奨理由追記） | #37, #40, #41 |
| #31 | vibemux（本体スクリプト） | なし |
| #30 | なし（OPEN だが設計 Issue、変更ファイルなし） | — |

### 結論

```text
今すぐ並列起動:
  - #37（specification.md 骨格）← #26/#34 マージ済みでブロック解除
  - #38（README.md ヒーロー文） ← #26 マージ済みでブロック解除
  - #39（README.md 並列ガイド） ← #26 マージ済みでブロック解除
  ※ #37 は specification.md、#38/#39 は README.md → 3者並列可
  ※ #38（冒頭）と #39（末尾新セクション）は同一ファイルだが別セクション

  - #31（PATH shim 実装）        ← #34 マージ済みで設計前提満たされた

#37 マージ後（並列起動）:
  - #40（VSCode 位置づけ追記）
  - #41（vibemux ポジショニング追記）
  - #42（Ghostty + tmux 推奨理由追記）
```

### 保留

- #30: OPEN だが設計完了済み（コメントで4問回答済み）。変更ファイルなし。downstream の blocking にはならない。

### 注意点

- #38 と #39 は同一ファイル（README.md）の異なるセクションを変更する。セクション境界が明確であれば並列可だが、PR マージの順序次第でコンフリクトの可能性は残る。リスクを嫌うなら #38 → #39 の直列でも可。

## 2026-04-25 — Issue #30, #40, #41, #42 並列実行判定（specification.md 現状確認版）

### 対象 Issue

#30, #40, #41, #42

### 前提変化（今回確認時点）

- specification.md を実読したところ、#37 相当の骨格（推奨実行環境セクション・VSCode 位置づけ・vibemux のポジショニング・Ghostty+tmux セクション）がすでに main の specification.md（50〜76行目）に記述済み
- 前回判断「#37 マージ後に #40/#41/#42 を並列起動」の前提条件が実質解消されている
- オープン PR はゼロ（競合ブランチなし）

### ファイル競合マトリクス

| Issue | 変更対象ファイル | 変更箇所 | 競合 Issue |
|-------|----------------|---------|------------|
| #40 | docs/specification.md | 「VSCode の位置づけ」セクション（68-70行目周辺）への追記 | #41, #42（同一ファイル・別セクション） |
| #41 | docs/specification.md | 「vibemux のポジショニング」セクション（72-76行目周辺）への追記 | #40, #42（同一ファイル・別セクション） |
| #42 | docs/specification.md | 「Ghostty + tmux」セクション（61-64行目周辺）への追記 | #40, #41（同一ファイル・別セクション） |
| #30 | なし（OPEN だが設計 Issue、変更ファイルなし） | — | — |

### 結論

```text
今すぐ並列起動:
  - #40（VSCode 位置づけ追記）
  - #41（vibemux ポジショニング追記）
  - #42（Ghostty + tmux 推奨理由追記）
  ※ #37 相当の骨格が specification.md に既存のため着手可
  ※ セクション境界が明確なため 3件並列可
  ※ PR マージは 1件ずつ順次実施してコンフリクトを回避すること

#40/#41/#42 マージ後:
  - #30 クローズをファウンダーに確認（blocking ではないが推奨）
```

### 保留

- #30: OPEN だが設計完了済み。変更ファイルなし。#40/#41/#42 の blocking にはならない。

### 注意点

- 3者が同一ファイルを編集するため、PR マージは 1件ずつ順次実施し、後続は main から rebase してコンフリクトを解消する運用を推奨
- セクション境界は specification.md の見出しで確定しているため、セクション内容の重複コンフリクトは発生しない

## 2026-04-25 — Issue #32 / #65 並列実行判定

### 対象 Issue

#32（VIBEMUX_PANE_TOP_LEFT 削除・2ペイン化）、#65（インストール手順の簡素化）

### 判断内容

**直列チェーン（#32 → #65）**

両 Issue が `README.md` と `README.en.md` を同時に変更するため、並列実行不可。

- #32: `vibemux` 本体（`split-window -v` 削除、`PANE_TOP_LEFT` 関連削除）、`README.md`、`README.en.md`（`PANE_TOP_LEFT` 設定例・テーブル行削除）、`tests/test_vibemux.sh`（テスト削除）
- #65: `README.md`、`README.en.md`（インストール手順の簡素化）

変更セクションは README 内で別箇所だが、#32 が複数行削除を伴うため行番号がずれ、#65 の diff と高確率で衝突する。

論理的順序としても「#32 で構造を確定 → #65 でインストール手順を整える」が自然。

### コンフリクトリスク

| ペア | ファイル | リスク |
|------|---------|--------|
| #32 / #65 | README.md、README.en.md | 高 |

### MVV 観点

- #32: Value #2「フローを切らない」（不要な3ペイン目を削除してシンプルに）
- #65: Value #1「一発で整う」（インストール手順の簡素化）
- 両者ともMVVに直結。優先度は #32 が先（構造確定が前提）
